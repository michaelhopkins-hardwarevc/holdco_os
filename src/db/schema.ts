// Drizzle schema for HoldCo OS.
//
// No tables are defined yet — this is the scaffold milestone. The Phase 1
// tables (organization, entity, user, membership, client, project, phase,
// time_entry, invoice, ...) and later-phase scaffolding are added in the
// schema milestone per spec §6, each via a generated Drizzle migration.
//
// This empty module keeps `drizzle.config.ts` valid so `npm run db:generate`
// works the moment the first table is added.

export {};
